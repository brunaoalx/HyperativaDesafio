CREATE TABLE lote (
    id                INTEGER    PRIMARY KEY,
    nome              TEXT (200),
    data              TEXT (100),
    lote              TEXT (100),
    qtdeRegistros     TEXT (100),
    header            TEXT (100),
    trailer           TEXT (100),
    dataProcessamento TEXT (100),
    tipoLote          TEXT (100) 
);
